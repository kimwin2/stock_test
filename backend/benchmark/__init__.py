"""참고 채널 대조 — 우리 시그널이 레퍼런스를 얼마나 따라가는지 측정한다.

목적과 경계가 분명하다.

  목적: 우리가 **독립적으로 계산한** 수급/시황 신호가, 숙련된 트레이더의
        같은 날 판단과 얼마나 일치하는지 수치로 보고, 어긋난 지점을
        구조적 결함(섹터 사전 누락, 임계값 편향)으로 환원한다.

  경계: 레퍼런스의 문장·수치를 제품에 실어 나르지 않는다. 이 패키지는
        **개발용 도구**다. Lambda 에 배포되지 않고, S3 산출물에도
        들어가지 않으며, 사용자 화면에 어떤 형태로도 노출되지 않는다.
        여기서 나오는 건 "우리 로직을 어디서 고쳐야 하는가" 뿐이다.

  이유: 유료 상품이라 남의 코멘트에 의존할 수 없다. 의존하면 그 채널이
        멈추는 순간 제품도 멈추고, 저작권 문제도 따라온다. 그래서
        레퍼런스는 **정답지(평가용)** 로만 쓰고, 개선은 항상 우리 쪽
        계산 로직에 반영한다.

사용:
    python -m benchmark.compare --date 2026-08-01
    python -m benchmark.compare --date 2026-08-01 --flow path/to/flow_dashboard.json
"""
