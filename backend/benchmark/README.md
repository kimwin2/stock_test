# 참고 채널 대조 (개발용)

우리 수급·시황 신호가 숙련 트레이더의 같은 날 판단에 얼마나 수렴하는지
수치로 보고, 어긋난 지점을 **우리 로직의 결함**으로 환원한다.

## 경계 (먼저 읽을 것)

- 레퍼런스의 문장·수치는 **제품에 실어 나르지 않는다.**
- 이 패키지는 Lambda 에 배포되지 않고, S3 산출물·사용자 화면 어디에도 안 들어간다.
- 유료 상품이 외부 코멘트에 의존하면 그 채널이 멈출 때 제품도 멈춘다.
  그래서 레퍼런스는 **정답지(평가용)** 로만 쓰고, 개선은 항상 우리 계산 로직에 반영한다.

## 쓰는 법

```bash
cd backend
python -m benchmark.compare --date 2026-08-11      # 하루
python -m benchmark.compare --all                  # 레퍼런스에 있는 모든 날
python -m benchmark.compare --all --strict         # 같은 날 스냅샷 없으면 건너뜀
```

- 레퍼런스 경로: `REFERENCE_DAILY_DIR` (기본 `~/repo/stock_chat/data/daily`)
- flow 스냅샷은 `s3://stock-dashboard-data/history/flow/YYYY-MM-DD.json` 에서
  자동으로 받아 `_snapshots/` 에 캐시한다. 없으면 최신본으로 폴백하며 경고가 뜬다.

## 읽는 법

**격차를 세 종류로 나눠 본다. 처방이 다르기 때문이다.**

| 구분 | 뜻 | 처방 |
|---|---|---|
| 사전 누락 | `SECTOR_RULES` 에 그 섹터 자체가 없음 | 코드 결함 — 섹터 추가 |
| 순위 누락 | 사전엔 있는데 주도섹터로 안 뽑힘 | 임계값 문제 — 강도/상한 조정 |
| 계열 일치 | 우리가 더 좁게/넓게 분류했을 뿐 | **결함 아님** |

하루치 노이즈로 로직을 흔들지 않도록 **2일 이상 반복된 격차만** 조치 후보로 낸다.

## 함정 (겪은 것)

- **날짜가 다른 flow 와 견주면 섹터 비교는 무의미하다.** 다른 날 시장을 비교하는
  꼴이다. 도구가 경고하지만 무시하고 숫자만 읽으면 로직을 잘못 고치게 된다.
- **문자열 일치만 보면 맞힌 것도 놓침으로 센다.** 레퍼런스의 "반도체"가 본문상
  소부장이면 우리 '반도체장비'와 같은 대상이다. 이걸 놓침으로 세다가 멀쩡한
  로직을 반도체 쪽으로 틀 뻔했다. `SECTOR_FAMILIES` 가 그 방지 장치다.
- **레퍼런스는 미장도 다룬다.** 해외 종목을 '놓친 종목'으로 세면 커버리지가
  구조적으로 왜곡된다. `is_domestic` 으로 스코프 밖으로 분리한다.
- **분류 상위어(산업재·소재 등)는 매매 대상이 아니다.** 사전 누락으로 세면
  `SECTOR_RULES` 에 잡동사니를 추가하게 된다. `UMBRELLA_TERMS` 로 제외한다.
- **부정형은 섹터가 아니다.** 추출기가 "반도체 및 비반도체 차별화" 같은 리포트
  문구에서 '비반도체' 를 그대로 긁어온다. 그런 섹터는 존재할 수 없다 —
  `NON_SECTOR_TERMS` 로 제외한다.
- **한 항목에 두 섹터가 들어있는 표기가 많다** ("자동차 및 로봇", "PCB 및 기판",
  "네트워크(통신)"). 안 쪼개면 둘 다 맞힌 날도 놓침으로 센다. `split_compound`.
- **동의어를 안 풀면 멀쩡한 섹터가 '구조적 결함' 으로 보인다.** 실측: 정리 전
  사전 누락 32건 중 25건이 이름만 다른 것이었다(기판·PCB·리드프레임 = 우리
  반도체장비, 원자력 = 원전, 연료전지 = 연료전지/수소). 이 목록을 그대로 믿고
  섹터를 추가했으면 사전이 중복 섹터로 오염됐을 것이다.

## 자동 축적 (2026-08-09 설정)

| 쪽 | 방식 | 상태 |
|---|---|---|
| 우리 flow 스냅샷 | EventBridge `stock-pipeline-flow-hourly` (평일 8~20시 매시) 가 매 실행마다 `s3://stock-dashboard-data/history/flow/YYYY-MM-DD.json` 기록 | ENABLED |
| 레퍼런스 | stock_chat 의 `daily.yml`(매일) · `hourly.yml`(30분) cron | 초대 링크 갱신으로 복구 |

레퍼런스 `data/` 는 평문이라 커밋하지 않는 정책이다(암호화 번들로만 배포).
대신 텔레그램이 원문을 보관하므로 **언제든 재생성**할 수 있다:

```bash
cd ~/repo/stock_chat && python -m pipeline.run --weeks 1
```

### 조용한 죽음을 막는 장치

실제로 채널 초대 링크가 만료돼 crawl 이 죽었고, **6일치를 날리고도 아무도
몰랐다.** 그래서 대조 도구가 실행 첫머리에 레퍼런스 신선도를 먼저 확인한다.

- 3일 이상 묵음 → 경고 + 재수집 명령 안내
- 데이터 없음 → 명시적 경고
- **수집기 마지막 실행이 실패 → 즉시 경고** (`collector_failure`)

낡은 데이터로 결론을 내는 게 대조를 안 하는 것보다 나쁘기 때문이다.

세 번째가 왜 따로 필요한가: 앞의 둘은 **데이터 나이**를 본다. 수집기가 오늘
죽어도 어제치가 있으면 3일간 조용하다. 그 사이 "레퍼런스가 오늘을 안 다뤘나
보다" 로 오해하고 대조 결과를 그대로 믿게 된다. 2026-08-10 에 실제로 그랬다 —
invite 링크가 **또** 만료돼 crawl 이 죽었는데 데이터 나이는 1일이라 경고가
안 떴다. 그래서 수집기가 남기는 `data/state.json` 의 실행 결과를 직접 읽는다.

> invite 링크 만료는 반복된다(운영자가 6개월마다 채널을 새로 연다).
> 복구는 `~/repo/stock_chat/config.yaml` 의 링크 갱신 → `pipeline.run --weeks 1`.

## 현재 수준 (2026-08-10, `--strict`)

```
섹터      정확 33% · 계열포함 67%   (날짜 일치 1일 — 2026-08-09)
종목      유니버스 커버리지 100% · 후보 적중 0%
현금비중  비교 불가 (그날 레퍼런스에 수치 없음)
```

**표본이 1일이다.** 스냅샷 축적이 2026-08-09 에 시작돼 그 이전 39일은
`--strict` 가 전부 건너뛴다. 이전 README 의 `정확 15% · 계열포함 37%` 는
다른 날 flow 와 견준 값이라 폐기한다.

### 스냅샷 없이도 측정되는 것

`사전 누락`(그 섹터가 `SECTOR_RULES` 에 있는가)은 그날 우리 주도섹터와 무관해
**스냅샷 없이 전 기간을 감사할 수 있다.** 이게 지금 가장 신뢰할 수 있는 신호다.

```bash
python3 - <<'PY'
from collections import Counter
from benchmark.reference import load_reference, available_dates, family_of
from flow_signals.universe import SECTOR_RULES
OUR = {n for n, _ in SECTOR_RULES}
gap = Counter()
for d in available_dates():
    r = load_reference(d)
    for norm in r["sectors"]:
        if norm not in OUR and not (family_of(norm) & OUR):
            gap[norm] += 1
print(gap.most_common())
PY
```

40일 감사 결과(2026-05-06~08-09): 정리 전 32건 → 동의어·부정형·복합표기를
걷어내니 7건, 전부 1일짜리(비철금속·철강·기계·피팅·레저·양자컴퓨팅)로 조치
기준(2일) 미만. 유일하게 반복된 진짜 결함이 **네트워크/광통신 8일** 이었고,
`통신장비` 섹터 신설로 처리했다.

`순위 누락`은 같은 날 스냅샷이 있어야 하므로 계속 쌓이길 기다린다.
