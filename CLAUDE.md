# stock_test — 프로젝트 가이드 (AI 에이전트용)

이 파일은 Claude Code 등 AI 에이전트가 이 레포에서 작업할 때 따라야 하는 규칙을 정리한다.

## 프로젝트 개요

한국 주식 단타 트레이더용 대시보드. 세 개의 탭:

1. **급등·테마** — 네이버 금융 뉴스 + 다중 시그널 → Gemini 테마 분석 → 종목 시세
2. **수급·주도** — Fear & Greed 오실레이터, 주도 ETF Mansfield RS, 수급 빈집, 거래대금 강도 (TI), 외인/기관 섹터별 매수, 매수 후보 차트
3. **AI 브리핑** — 자체 시그널(F&G/주도섹터/수급/빈집) 변화 + DART 공시 이벤트를 Gemini 가 서술형 브리핑으로 작성 (`backend/briefing/`). 예측·조언 문장 금지, 데이터 서술만. DART_API_KEY 없으면 공시 섹션 스킵, LLM 실패 시 규칙 기반 fallback. S3 데이터에 briefing 키가 없으면 프론트는 `briefing_sample.json` 으로 샘플 미리보기 표시.

**스택**: Python(AWS Lambda) + vanilla JS(GitHub Pages) · 데이터: Naver mobile API, FinanceDataReader, Telegram(Telethon), Gemini (OpenAI SDK + Gemini OpenAI-호환 endpoint, model `gemini-3.5-flash-lite`)

**배포**:
- Lambda: 평일 8~16시 10분 간격(theme), 평일 8~20시 정각(flow)
- S3: `stock-dashboard-data` 버킷에 `dashboard_data.json` / `flow_dashboard.json`
- GitHub Pages: `frontend/` 정적 호스팅

## gstack 스킬 정책

이 레포는 gstack 5개 스킬만 사용한다. 다른 gstack 스킬은 `.claude/skills/gstack/` 에 물리적으로 존재하지만 **호출하지 않는다**. 초기 단계에서 노이즈를 줄이기 위함.

### 활성 스킬 5개

| Skill | 사용 시점 | 트리거 키워드 |
|---|---|---|
| **`/office-hours`** | 새 기능/탭/제품 결정 단계 — 페르소나·시점·결정 명확화 | "brainstorm this", "이거 해야 할까", "정보 어떻게 보여줄까" |
| **`/investigate`** | 버그 발생 시 — 임의 수정 금지, 근본 원인부터 | "debug this", "왜 안돼", "이전엔 됐는데" |
| **`/review`** | 커밋/푸시 직전 — 프로덕션 깨질 패턴 검사 | "review this", "코드 리뷰", "푸시 전 검토" |
| **`/qa`** | UI/기능 동작 검증 — 실 브라우저로 클릭하며 확인 | "qa", "test this site", "브라우저로 돌려봐" |
| **`/careful`** | 파괴적 명령 실행 직전 — 재확인 강제 | `rm -rf`, `git push --force`, `DROP TABLE` 등 자동 트리거 |

### 사용 안 하는 스킬 (참고용)

`.claude/skills/gstack/` 에 같이 들어있지만 **이 레포에서는 호출하지 말 것**:

- 계획류: `/plan-ceo-review`, `/plan-eng-review`, `/plan-design-review`, `/plan-devex-review`, `/autoplan` — 솔로 단계라 과함
- 출시류: `/ship`, `/land-and-deploy`, `/canary`, `/landing-report`, `/document-release` — PR 워크플로 아님 (main 직 푸시)
- 디자인 자동화: `/design-consultation`, `/design-shotgun`, `/design-html`, `/design-review`, `/devex-review` — 화면은 직접 결정
- 보조: `/codex`, `/cso`, `/health`, `/benchmark`, `/benchmark-models`, `/retro`, `/learn`, `/context-save`, `/context-restore`, `/skillify`, `/make-pdf`, `/setup-deploy`, `/setup-gbrain`, `/setup-browser-cookies`, `/freeze`, `/guard`, `/unfreeze`, `/gstack-upgrade`, `/qa-only`, `/browse`, `/open-gstack-browser`, `/connect-chrome`, `/pair-agent`

→ 향후 필요 시점이 명확해지면 위 표에 옮기고 사용. 지금은 5개로 한정.

### gstack 인프라 메모

- **Bun 미설치 상태**. `gstack/setup` 스크립트는 Bun 필수라 미실행. `bin/*` 스크립트는 모두 bash이므로 그대로 작동.
- **`/qa` 의 browse 바이너리**는 빌드 안 됨. `/qa` 호출 시 Playwright(파이썬) 폴백으로 진행하거나, 필요하면 그때 Bun 설치 후 `cd .claude/skills/gstack && ./setup` 실행.
- skill 자동 업데이트 체크: `~/.gstack/sessions/` 에 세션 파일 생성하지만 무해.

## 코드 작업 규칙

### 파이썬 (backend/)

- 신규 모듈은 `backend/<domain>/` 패키지로 묶는다 (`flow_signals/`, `price_signals/`, `telegram/`, `antwinner/`, `infostock/` 패턴 따라).
- 외부 API 호출은 `data_sources.py` 또는 `collector.py` 에 격리.
- Lambda 진입은 `handler.py` 에 통합. `event.mode` 로 분기 (theme / flow / 새 mode 추가 가능).
- 의존성 추가 시 `backend/requirements.txt` + Lambda 패키지 크기 확인 필수 (250MB unzipped 한도).

### 프론트 (frontend/)

- vanilla JS, no framework. 차트는 vanilla SVG 로 작성 (외부 라이브러리 추가 금지).
- 데이터 fetch URL 자동 분기: `github.io` 호스트면 S3, 로컬이면 같은 디렉토리 JSON.
- 새 탭 추가 시 `index.html` 의 탭 nav + 패널 + 별도 `*.js` 모듈로.

### 비밀

- `.env` (`backend/.env`, `.env`) 는 .gitignore 됨. 신규 키 추가 시 `backend/.env.example` 도 업데이트.
- `TG_STRING_SESSION` 같은 장기 자격증명은 절대 커밋 금지.

### Git

- main 직 푸시 워크플로. PR 도입 전. 그러나 **force-push 는 명시 허락 없이 금지**.
- 커밋 메시지: 한글 가능. `feat:` / `fix:` / `chore:` prefix 권장하지만 강제 아님.
- 외부 자료(`etc_source/`)는 .gitignore 됨. 절대 커밋 금지.

## 실행 명령 모음

```bash
# 백엔드 로컬 실행 (theme)
cd backend && python main.py

# 백엔드 로컬 실행 (flow)
cd backend && python -m flow_signals.pipeline

# 프론트 로컬 서버
cd frontend && python -m http.server 8080 --bind 127.0.0.1

# Lambda 배포
sam build && sam deploy --parameter-overrides "GeminiApiKey=AIza..."

# 텔레그램 채널 덤프 + 분석 (개인 분석 도구)
cd backend && python -m telegram.fetch_dump --channel "https://t.me/+..." --limit 1000 --out telegram/dev/<name>_raw.json
cd backend && python -m telegram.analyze_dump --in telegram/dev/<name>_raw.json

# 참고 채널 대조 (개발용 — 자세한 내용은 backend/benchmark/README.md)
cd backend && python -m benchmark.compare --all

# flow 수동 실행 (스케줄은 평일 8~20시라 주말·장외에는 안 돈다)
printf '{"mode":"flow"}' > /tmp/p.json
aws lambda invoke --function-name stock-pipeline --invocation-type Event --payload file:///tmp/p.json /tmp/out.json
```

> **주의**: 섹터 분류·주도섹터 로직을 고치면 반드시 배포 후 `mode=flow` 로 돌려
> `candidateFilterStats` 의 `beforeFilter`/`afterFilter`/`scoreCutoffRelaxed` 를
> 이전 값과 비교할 것. 코드만 보고 끝내면 후보가 30% 줄어든 걸 놓친다 (실제로 겪음).

## 참고 채널 대조 (backend/benchmark/)

수급·시황 탭이 숙련 트레이더의 판단에 얼마나 수렴하는지 **측정**하는 개발용 도구.

- **경계**: 레퍼런스의 문장·수치를 제품에 실어 나르지 않는다. Lambda 배포·S3 산출물·
  사용자 화면 어디에도 들어가지 않는다. 유료 상품이라 외부 코멘트에 의존할 수 없고,
  의존하면 그 채널이 멈추는 순간 제품도 멈춘다. 정답지(평가용)로만 쓴다.
- **개선 경로**: 대조에서 나온 격차는 항상 **우리 계산 로직**에 반영한다.
  `사전 누락`(SECTOR_RULES 에 그 섹터 자체가 없음) → 코드 결함,
  `순위 누락`(사전엔 있는데 주도섹터로 안 뽑힘) → 임계값 문제. 처방이 다르다.
- 하루치 노이즈로 로직을 흔들지 않도록 **2일 이상 반복된 격차만** 조치 후보로 낸다.

```bash
cd backend
python -m benchmark.compare --date 2026-08-01
python -m benchmark.compare --all --flow-dir <일별 스냅샷 디렉터리>

# 매일 저녁 관심종목 대조 (평일 KST 20:35 자동 — daily-benchmark.yml)
python -m benchmark.daily_check --days 5
```

`daily_check` 는 `compare` 와 목적이 다르다. 적중률 한 줄이 아니라 **빗나간
종목마다 어느 관문에서 떨어졌는지**(유니버스 밖 / 주도섹터 밖 / 섹터 상한 /
조건 탈락)를 붙인다. 관문마다 처방이 달라서다. 조치는 **2일 이상 반복된
격차**만 — 리포트 끝에 따로 뽑아준다.

> CI 에는 stock_chat 체크아웃이 없다(레퍼런스 `data/` 는 평문이라 커밋 안 함).
> 대신 stock_chat 이 **이미 매시간 공개하는 암호화 배포 번들**(GitHub Pages 의
> `data/core.enc`)을 `benchmark/stock_chat_bundle.py` 가 받아 복호화한다 —
> stock_chat 쪽 변경 없이 이 레포에 `SHARE_PASSPHRASE` 시크릿 하나만 넣으면
> 붙는다. 없으면 '레퍼런스 없음' 리포트를 내고 통과한다.
> 절차는 `backend/benchmark/STOCK_CHAT_BRIDGE.md`.
>
> 번들에는 요약 문장·원문 인용·stock_chat 의 API 키까지 들어있다. `_slim()` 의
> **화이트리스트**(`date`/`stance`/`cash.kr`/`sectors`/`tickers`/`message_count`)
> 로만 꺼낸다. 블랙리스트로 바꾸지 말 것 — 저쪽이 필드를 추가할 때마다 샌다.

> **수집이 조용히 멈춘다.** 운영자가 초대 링크를 수시로 폐기해서 `stock_chat` 크롤이
> 에러 없이 이틀간 멈춰 있었다(2026-08-09~11). 링크가 죽어도 **이미 가입된 계정은
> 채널을 계속 읽을 수 있으므로** `config.yaml` 채널 항목에 `chat_id` 를 병기했고
> `resolve_channel` 이 링크 실패 시 그쪽으로 폴백한다. 대조 전에 `data/daily/` 의
> 최신 날짜부터 확인할 것 — 오래된 정답지로 로직을 흔들면 안 된다.

레퍼런스 경로는 `REFERENCE_DAILY_DIR` 환경변수(기본 `~/repo/stock_chat/data/daily`).
flow 산출물은 매 실행마다 `s3://<bucket>/history/flow/YYYY-MM-DD.json` 로 스냅샷이
남으므로, 같은 날짜끼리 대조하려면 이걸 받아서 `--flow-dir` 로 준다. 날짜가 어긋나면
도구가 경고한다 (다른 날 시장을 견주면 섹터 비교가 성립하지 않는다).

## 알려진 이슈 / 메모

- KOSDAQ 시총 200위 밖 종목 (심텍, 코리아써키트 등)은 매수 후보에 안 잡힘. 필요 시 `flow_signals/universe.py` 의 `EXPLICIT_SECTOR` 에 추가.
- `pykrx` 는 KRX 로그인 요구로 종목별 투자자 데이터 못 가져옴. `Naver mobile API` (`/api/stock/{code}/trend`, 10일치) 로 대체.
- 일부 ETF 코드(`471490`, `421970`, `381190`, `117710`)는 FDR 데이터 부재 — 무시하고 진행.

### 매수 후보 선정 규율 (2026-08 개편)

후보 품질 저하(미달 종목 유입)를 막기 위한 하드 규칙. 완화 시 후보가 다시 오염되므로 신중히.

- **섹터 분류**: `금융` 을 `증권`/`은행`/`보험` 으로 분리. 하나로 묶으면 증권 ETF 강세에
  보험사(롯데손해보험 등)까지 주도섹터로 딸려 들어온다. `"지주"` 키워드는 세아베스틸지주(철강)를
  오분류해 `"금융지주"` 로 좁혔다.
- **주도 섹터 상한**: 세 축(ETF RS / 수급 강도 / 거래대금 쏠림) 합쳐 최대 7개(`MAX_LEADING_SECTORS`).
  수급 섹터는 **절대 금액이 아니라 시총 정규화 강도**(만분율, `FLOW_STRENGTH_MIN=5.0`) 기준.
  절대 금액 기준이던 기존 로직은 주도섹터를 13개까지 불려 필터를 무력화했다.
- **축은 이어붙이지 말고 번갈아 뽑을 것**(2026-08-11). ETF→수급→거래대금 순으로
  concat 한 뒤 뒤에서 자르면 **항상 거래대금 축부터 죽는다**. 축 순서는 신호 세기와
  무관한데 그게 우선순위가 되어 있었다. 실측: ETF 축이 4개를 만든 날 거래대금 축의
  반도체장비(그날 시장이 하루 종일 민 코스닥 소부장)가 상한 밖으로 밀려 사라졌다.
  → 각 축 1순위 → 각 축 2순위 … 순으로 채운다. 축별 상한(`FLOW_SECTOR_TOP_N`,
  `TURNOVER_TOP_N`)은 총량을 통제하지 않으므로 넉넉히 둔다 — 빡빡하면 7자리를
  다 못 채우고 끝난다(실측 5개만 채워져 바이오가 두 축 모두에서 간발로 탈락).
- **ETF→섹터 순서는 RS 강도순**. 점수 로직이 `leading_sectors.index()` 로 1·2·3위에
  +40/+32/+24 를 주므로, 가나다순이면 가산점이 이름 순서로 배분된다(과거 실제 버그).
- **하드 필터**(`Step 7a`): `aboveMA10`(추세 생존) ∩ `oscLast < 0`(빈집 정식 정의) ∩
  점수 45점 이상 ∩ 섹터당 최대 4종목(**주도섹터 상위 3개는 7종목**). 후보가 8개
  미만이면 **점수 커트라인만** 완화한다 (추세·빈집은 전략의 정의라 완화 금지).
- **섹터 상한을 주도섹터에까지 똑같이 걸지 말 것**(2026-08-11). 전 섹터 일괄 4개였는데
  이 규칙 하나가 탈락의 절대다수를 만들었다 — 78개 중 32개가 여기서 잘렸고(추세 5 /
  빈집 17 / 점수 1 을 다 합친 것보다 많다) 그중 18개가 반도체장비였다. 하필 그날의
  주도 테마다. 주도섹터를 뽑아놓고 그 1위 섹터를 가장 세게 자르면 전략이 뒤집힌다.
  분산은 수단이지 목적이 아니다.
- `exitSignals` 는 하드 필터 **이전** 풀(`pre_filter_candidates`)에서 산출한다. 필터 후 풀로
  계산하면 "10MA 이탈" 조건이 영원히 성립하지 않는다.

### 빈집 판정은 부호가 아니라 감속이다 (2026-08-11)

후보 풀을 만들 때 `institutionNet5d < 0`(5일 순매도) 로 먼저 자르고 **그 다음에**
수급 오실레이터를 계산하고 있었다. 이건 우리가 내세운 정의와 다른 조건이다.

오실레이터는 MACD 히스토그램이다 — 순매수의 **부호가 아니라 감속**을 잰다.
5일 순매수가 플러스여도 20일 페이스보다 느려졌으면 osc 는 음수다. 그게 히스토그램을
쓰는 이유 전부인데, 선필터가 그 경우를 통째로 지웠다.

- 실측: 참고 채널이 "수급 빈집" 이라 지목한 5종목 중 3개가 5일 순매수 **플러스**였다
  (파마리서치 +519억, 에이피알 +136억, 코스맥스 +141억). 그중 파마리서치는
  `vacancyScore` -6.3e-4, 정식 osc -0.00097 로 명백한 빈집인데 이 한 줄에 걸려
  사라졌고, 그날 그들이 실제로 편입한 종목이었다.
- 조치: 선필터 제거. 주도섹터 교집합만 걸고 빈집 판정은 `Step 7a` 의 `osc < 0` 에 맡긴다.
  정렬 키 `vacancyScore` 자체가 이미 감속 지표라 순매도 종목이 자연히 앞에 온다.
- 대신 오실레이터를 계산할 종목 수가 필요해 풀을 80 → 120 으로 넓혔다.
  **종목당 60일 시계열 fetch 라 비용이 선형으로 는다** (실측 247s→379s / Timeout 600s).
  더 늘리려면 반드시 실측 먼저.

### 섹터 수급은 기관 기준으로 본다 (2026-08-11)

외인/기관 중 **더 강한 쪽**(max)을 섹터 대표값으로 쓰고 있었다. 그러면 한 주체만
사고 다른 주체는 파는 섹터가 1위로 올라온다. 실측: 수급축 1위 `유통/음식료` 가
외국인 단독 신호(외인 36.0 / 기관 **-45.7**)였고, 그 결과 후보 4자리를 신세계·이마트·
하림지주(내수 유통)가 채웠다. 그날 시장이 본 '소비재'는 화장품·음식료 수출이었다.

- **교집합(양쪽 모두 순매수)을 하드 조건으로 걸면 축이 죽는다.** 실측에서 통과 섹터가
  3개로 줄고 그날의 진짜 1위(반도체장비)까지 탈락했다 — 외국인이 삼성전자 하나를
  사려고 나머지 전 섹터를 파는 날이 흔하다. 외국인 부호를 거부권으로 쓰면 안 된다.
- 조치: **순위를 기관(organ) 강도로 매긴다.** 국내 섹터 로테이션을 만드는 주체가
  기관이고, 참고 채널도 업종 순위를 사모/투신/연금(전부 기관 내부 주체)으로 뽑는다.
  금액 하한도 기관 기준 — 합계로 보면 기관이 2,538억 담은 반도체장비가 외국인
  매도에 상쇄돼 -1,921억으로 뒤집힌다.
- 효과: 외인 단독 artifact(유통/음식료·항공·해운)가 전부 사라지고 순위가
  반도체장비 20.4 / 화장품·소비재 18.0 / 2차전지 17.6 / 건설·인프라 17.4 /
  화학 14.6 / 바이오 13.8 / 전력기기 9.9 로 정리됐다.
- `FLOW_SECTOR_MIN_MEMBERS` 3 → 5. 정확히 3종목짜리 묶음(항공·해운)이 계속 상위를
  차지해 진짜 주도섹터를 상한 밖으로 밀어냈다. 5면 신재생(5)·통신장비(6)·원전(6) 은 산다.

### ETF 축으로 재현할 수 없는 섹터가 있다 (2026-08-11, 시도 후 되돌림)

참고 채널이 주도업종을 "섹터지수 10주·10일 이평 상회" 로 고르길래 같은 조건을
ETF leading 판정에 추가해봤다. **실패했고 되돌렸다.**

- 목표였던 전력·원전 ETF 는 3개월 -36% 낙폭이라 50일선 아래 — 여전히 탈락
- 대신 철강·게임·에너지화학이 무더기 통과해 주도섹터 7자리를 채웠고, 그날의
  진짜 주도(반도체장비)가 상한 밖으로 밀려났다
- 원인은 RS 창 길이가 아니라 **바스켓 구성**이었다. 그들의 '데이터센터' 는 LS·대한전선·
  일진전기 같은 개별 강세주 묶음이고, 우리 전력 ETF 는 낙폭과대주까지 담은 지수다.
- `aboveMA10w` / `aboveMA10d` / `rsShort` 필드는 관측용으로 남겼다. **판정에는 쓰지 말 것.**

### 섹터 분류 — 가장 조용하고 큰 결함 (2026-08-09)

`classify_sector` 는 **회사명에 업종 키워드가 있을 때만** 동작한다. 한국 기업명에는
업종이 잘 안 들어가서, 이 방식만으로는 절반이 '기타'로 빠진다.

- 실측: 유니버스 550종목 중 327개(59%), **시총 기준 15.6%** 가 미분류였다.
  LG에너지솔루션(84조)이 2차전지가 아니었고 두산에너빌리티(49조)가 원전이 아니었다.
- 섹터 수급은 종목을 섹터별로 **합산**해 구한다. 대장주가 빠지면 그 섹터 합계가
  통째로 어긋나고, 주도섹터 판정과 후보 필터가 전부 그 위에 서 있다.
- 조치: 시총 상위 미분류를 `EXPLICIT_SECTOR_ADDITIONS` 에 코드로 직접 지정.
  현재 시총 기준 미분류 3.2%.
- **새 종목이 시총 상위로 올라오면 이 목록도 늘려야 한다.** 정기 점검 필요.

**섹터를 잘게 쪼개면 반대 방향 함정이 열린다.** 정규화 강도(시총 대비)는 시총이
작을수록 커지므로, 소형 섹터가 상위를 독식한다. 실측: 항공(3종목)이 강도 43.3 으로
1위에 올라 반도체장비·원전을 밀어내고 후보를 39→21개로 깎았다.

**하드 컷오프로 자르려다 두 번 실패했다.** 경계값에서 절벽이 생겨 어느 쪽으로
잡아도 틀린다 — 기준 6이면 신재생(5종목)까지 잘리고, 기준 5면 신재생(강도 64.9)이
반도체장비(39종목·2,492억)를 밀어내 주도섹터 커버리지가 86→54종목으로 준다.
→ 순위를 `강도 × min(1, 종목수/10)` 으로 매긴다(`FLOW_SECTOR_BREADTH_FULL`).
   항공 43.3→13.0, 통신 18.0→7.2, 해운 15.6→4.7 로 절벽 없이 강등된다.
   3종목 미만은 애초에 섹터가 아니므로 그대로 제외.

**어설프게 묶느니 '기타'로 두는 편이 낫다.** 업종이 다른 종목을 한 버킷에 넣으면
(강원랜드+농심+CJ대한통운) 없던 섹터 강도가 만들어져 가짜 주도섹터가 된다.

**반대로, 큰 섹터에 파묻힌 소수 종목도 결함이다** (2026-08-10). 광통신주
(대한광통신·오이솔루션·성호전자·RFHIC)가 `반도체` 에 들어 있었다. 반도체 합계는
2,643조라 이 6.8조는 0.21% — 이 테마가 단독으로 뛰어도 섹터 수급에 아무 흔적이
안 남는다. 대조에서 `네트워크/네트워킹/광통신/통신장비` 가 8일 이상 최우선
주도업종으로 반복 지목됐는데 우리는 한 번도 잡지 못했다. → `통신장비` 신설(6종목).
이통3사(`통신`)와는 별도다 — 배당·요금제가 아니라 데이터센터 트래픽으로 움직인다.
`SECTOR_RULES` 에서 **`통신` 규칙보다 반드시 위**에 둔다 (아래면 '대한광통신'이
`"통신"` 에 먼저 걸린다).

### ETF 코드↔라벨 어긋남 — 주도섹터 전체를 조용히 뒤집던 결함 (2026-08-10)

`THEME_ETFS` 는 `코드 → 손으로 쓴 라벨` 이다. 그런데 **RS 는 코드의 실제 주가로
계산하고, 섹터 매핑은 라벨의 키워드로 한다.** 둘이 어긋나면 엉뚱한 ETF 의 강도로
엉뚱한 섹터가 주도로 뽑힌다. 전수 대조하니 **46개 중 25개가 어긋나 있었다.**

- `479850` 은 `TIGER 글로벌AI인프라` 가 아니라 **HANARO K-뷰티** 였다.
  K-뷰티 ETF 가 강한 날 `AI/반도체팹리스` 가 주도섹터 2위로 올라갔다.
  (CLAUDE.md 가 오래 기록해 온 "글로벌AI인프라 다중 매칭" 사고도 실은 이 ETF 가
   아니었다 — 존재하지 않는 라벨을 놓고 원인을 찾고 있었다.)
- `266390` 은 `KODEX 2차전지산업` 이 아니라 **KODEX 경기소비재** 였다.
- `098560`(KODEX 자동차)·`456600`(KODEX K-방산)은 **죽은 코드**였다.

**발견 경로가 중요하다.** 코드만 읽어선 절대 안 보인다. ETF 실제 편입비중(PDF)을
붙이자마자 "신세계가 KODEX 2차전지산업 편입 6.69%" 라는 말이 안 되는 줄이 떠서
드러났다. **외부 사실과 대조해야만 보이는 결함이 있다.**

- 조치: 코드 전수 정정 + `resolve_theme_etfs()` 가 매 실행 상장 목록으로 라벨을
  덮고 어긋남을 출력한다. 운용사가 코드를 재활용하므로 이 목록은 또 썩는다.
- 해외 ETF 는 전부 제거했다. 한국 섹터로 매핑될 수 없어 `기타` 로 버려지면서
  RS 상위만 차지해 국내 테마를 밀어냈다 (2026-08-10 주도 ETF 1위가 미국 S&P500).
- **ETF 를 추가할 땐 반드시 상장 목록에서 이름을 확인하고 넣을 것.**
  그리고 `sector_map` 에 그 이름의 키워드가 있는지 같이 확인할 것 —
  `HANARO 원자력iSelect` 는 `"원전"` 키워드에 안 걸려 매핑이 없었다.

### 주도섹터 판정 함정 (2026-08-07 실사고)

- **ETF 키워드 매칭은 첫 매칭에서 `break`**. 없으면 ETF 하나가 여러 섹터를 만든다.
  실제: `TIGER 글로벌AI인프라` 가 `"AI"` 와 `"인프라"` 에 동시 매칭돼 건설/인프라가
  RS 78.9 짜리 3위 주도섹터로 둔갑 → 포스코인터내셔널·대우건설·동원시스템즈가
  매수 후보 1·2·3위를 차지했다.
- **`NON_THEME_SECTORS = {보험, 은행}`**. 금리·밸류업으로 장기 RS 는 올라오지만
  단타 테마로 움직이지 않는다. KODEX 보험(RS 76.5)이 주도섹터가 되자 후보 11개 중
  4개가 보험사(롯데손해보험 등)로 채워졌다. 증권은 거래대금 테마라 유지.
- **수급 섹터는 강도와 절대금액을 모두 요구**(`FLOW_AMOUNT_MIN=300억`).
  절대금액만 보면 대형주 섹터가, 정규화 강도만 보면 초소형 섹터가 독식한다.

### 급등·테마 탭 — 정원이 가짜 테마를 만든다 (2026-08-11)

카드 개수를 미리 정해 두면 그 자리는 **반드시** 채워진다. 근거가 있든 없든.

- 실사고: 7개 정원 중 실측 근거가 있는 테마가 5개뿐인 날, `조선기자재`(지역난방공사
  +4.31%·산일전기 +0.49%·일진전기 **-1.28%**)와 `게임`(NC +4.93%·컴투스 +3.19%·
  데브시스터즈 **-2.00%**)이 남은 두 칸을 메웠다. 급등 테마 탭에 급등이 없었다.
- `조선기자재` 카드에는 조선기자재가 한 종목도 없었다(전부 전력기기·집단에너지).
  LLM 이 만든 테마명은 시세가 확인해 주지 않으면 그냥 문장이다.
- **테마 생존 조건이 "선정 종목 중 최고 등락 ≥ 3%" 하나였다.** 대장주 1종목이
  +3% 만 되면 나머지가 마이너스여도 통과한다. 개미승리는 평균 등락률과 상승비율을
  같이 보는데 이쪽은 안 봤다.

조치 (3개를 함께 걸어야 막힌다 — 하나만으로는 다른 경로로 샌다):

1. **정원 폐지.** 프롬프트·docstring·로그에서 "7개"를 전부 상한으로 바꿨다.
   최종 개수는 `stock_data._select_theme_stocks` 의 게이트가 정한다. 하한은
   `MIN_THEMES=3` 완화 재선정으로만 방어.
2. **외부 근거 없는 테마는 기준을 올린다.** 선정 종목 전부가 `뉴스분석` 단독이면
   (`ts.is_unbacked`) 대장주 등락 기준이 3% → **10%**(완화 시 5%). 개미승리·
   급등클러스터·인포스탁·와우넷·유튜브·텔레그램 중 **하나라도** 그 종목을 지목했으면
   기존 기준을 쓴다. 오늘 데이터에서 정확히 문제의 2개만 떨어지고 5개는 남았다.
3. **등락률 가중치 상향 + 거래대금 상한.** 등락 ×1.2 → **×2.5**, 거래대금
   ×8 무제한 → **×5, 상한 12점**. 이전 배점에서는 산일전기 +0.49%(1,848억)가
   18.7점으로 지역난방공사 +4.31%(751억) 20.2점과 사실상 동점이었다 —
   대형주가 거래대금으로 대장주 자리를 사고 있었다.

4. **두 글자 섹터어 병합 불능 수정.** 같은 날 `건설 중소형`과 `건설 및 토목 자재`가
   따로 떴다. 둘 다 근거는 멀쩡했지만(인포스탁 1위 / 급등클러스터 333점) 같은 섹터다.
   `_theme_matches` 의 토큰 생성이 **3글자 슬라이스뿐**이라 `len(t) >= 2` 조건이 죽은
   코드였다 — `반도체 A`↔`반도체 B` 는 병합되는데 `건설 A`↔`건설 B` 는 영원히 안 된다.
   한국 섹터명은 두 글자가 압도적이다(건설·조선·방산·원전·해운·게임·증권).
   → `_sector_tokens()` 로 낱말 단위 비교를 넣되, **낱말 공유 AND 종목 겹침 ≥ 0.3**
   두 근거를 동시에 요구한다. 임계값 하나(0.6→0.5)를 내려 맞추면 그날 숫자에만
   맞는 조정이 되고, 낱말만 보면 `2차전지 소재`와 `반도체 소재`가 붙는다.
   병합 시 `mergedThemes` 를 `build_candidates` 가 같이 조회하게 했다 — 안 그러면
   흡수된 테마를 지목한 실측 소스 태그가 사라져 근거가 LLM 단독으로 바뀐다.

`sector_mix()` 는 테마명↔실제 업종 불일치를 계산하지만 **로그로만 남기고 아무것도
막지 않는다**. 위 조치는 "안 오른 테마"를 막을 뿐 "이름이 틀린 테마"를 직접
막지는 못한다. 다음에 같은 증상이 보이면 여기부터 볼 것.

### 장세는 테마가 아니다 — 외부 시그널을 검증 없이 강제 삽입하면 안 된다 (2026-08-18)

테마 탭에 **`하락장`** 카드가 떴다. 안에는 코데즈컴바인(의류)·신라섬유(섬유)·
양지사(문구)가 들어 있었다. 업종 공통점이 없다. 애초에 "하락장" 은 테마가
아니라 **그날 장이 어땠는지**를 말하는 이름이다.

경로: 개미승리 `/api/all-themes` 카테고리에는 업종 버킷과 **장세 버킷이 섞여**
있다. 그날 `하락장` 이 등락률 2위였고, `_apply_antwinner_top2_postprocess` 가
상위 2개를 **이름 검증 없이 강제 삽입**한다. 그대로 카드가 됐다.

- **가짜 테마 하나가 진짜 테마 하나를 밀어낸다.** 같은 날 급등클러스터가
  `의류 및 패션`(134점 · 좋은사람들·코데즈컴바인·인디에프 **동반 상한가**)을
  올렸는데 자리를 못 얻었다. 강제 삽입은 슬롯을 뺏는 동작이다.
- **`_from_antwinner` 는 면죄부가 아니다.** 강제 삽입된 테마는
  `_find_replaceable_theme_index` 의 교체 대상에서 빠지도록 되어 있어,
  출처를 존중하는 순간 이 사고는 되돌릴 방법이 없어진다.
- **프롬프트 지시로는 안 막힌다.** SYSTEM_PROMPT 는 이미 "억지 테마 금지",
  "거시 이슈 배제" 를 적어두고 있었지만 이 경로는 LLM 을 거치지도 않는다.
  (LLM 이 지어내는 경로도 따로 있다 — 2026-08-11 '조선기자재' 참고.)

조치 — **두 곳에서 자른다**:

1. `antwinner/collector.py` 의 `NON_THEME_BUCKETS` — 수집 단계에서 뺀다.
   **상위 N 을 자르기 전에** 걸러야 한다. 자른 뒤에 빼면 그 자리가 그냥 비어
   진짜 테마 하나를 손해 본다.
2. `analyzer.py` 최종 스윕 — `is_non_theme_bucket` 으로 **출처와 무관하게**
   한 번 더 자른다. 1번은 개미승리 경로만 막고, LLM·인포스탁 경로가 남는다.

걸러내는 축은 셋이다 — **장세**(하락장·지수방어·순환매) / **매매 스타일**
(개별주·급등주·낙폭과대) / **가격 패턴**(상한가·저가주·실적).
재무 속성(저PBR·고배당)과 유통물량(품절주)은 **일부러 안 넣었다** — 그건
"왜 올랐나"에 실제로 답하는 근거라 테마로 볼 여지가 있다.

> 목록을 추측으로 늘리지 말 것. 수집기가 `[제외]` 로그에 실제 버킷명을
> 찍으므로, 그 로그에 뜬 것만 보고 늘린다. 상대 사이트의 카테고리 체계는
> 우리가 모르고, 짐작으로 넣으면 멀쩡한 업종을 지운다.

### 사명 접두사를 계열 근거로 쓰면 안 된다 (2026-08-11)

`price_signals` 급등 클러스터는 시드 종목명들의 **2~4자 공통 조각**을 뽑아, 그
조각이 이름에 들어간 종목에 +3.0 을 줬다(통과선 2.5). 조각 하나로 통과선을 넘는다.

- 실사고: 금호건설·금호건설우가 만든 조각 `금호` 에 **조명업체 금호전기**가 걸려
  '건설 및 토목 자재' 테마에 편입됐다. 상한가(+5)·저가(+4) 보너스까지 얹혀 12.0 점.
  두 회사는 계열 관계조차 아니다(금호전기 계열사는 금호에이엠티·브릭메이트).
- 같은 결함이 `_prune_candidates_with_llm` 에서도 작동했다. 조각 공유 3종목이면
  `strong_family` 로 **LLM 검수를 통째로 건너뛴다** — 금호건설·금호건설우·금호전기가
  '금호' 로 3종목이라 이 테마는 검수도 면제받았다.

조치: 공통 조각을 **그룹 안 어느 종목의 사명 전체**일 때만 인정한다.
`금호건설`(금호건설·금호건설우에 포함) 은 계열, `금호` 는 아니다.
`위메이드`↔`위메이드맥스`↔`위메이드플레이`, `한화`↔`한화시스템` 은 그대로 유지된다.
삼성·SK·LS·한화 같은 접두사 공유는 전부 여기서 끊긴다.

- 모멘텀 보너스(상한가+5·저가+4)는 **시드 지목을 뺀 근거**가 있을 때만 준다.
  `_select_theme_stocks` 는 LLM 묶음이 맞는지 검증하는 자리다. 검증 대상인 LLM
  지목을 근거 삼아 +9 를 얹으면 검증이 성립하지 않는다.
- 곁가지로 드러난 비대칭: 기사를 **고를 땐** 제목+요약으로 찾으면서 **문맥으로는
  제목만** 남기고 있었다. 요약에만 업종어가 있는 기사가 흔해(제목 "서산, 수요 증가에
  급등" / 요약 "레미콘 건설 자재") 정상 종목의 근거가 증발했다. 찾을 때 쓴 텍스트를
  근거로도 쓴다.

**업종 판정에 외부 사실이 없다는 게 근본 한계다.** 서산(콘크리트)이 맞고
금호전기(조명)가 틀렸다는 건 네이버 기업개요를 봐야 안다. 사명·기사 문맥만으로는
경계가 늘 흐리다. 의심되면 `finance.naver.com/item/main.naver?code=` 의 기업개요를
직접 확인할 것 — 코드만 읽어서는 판단이 안 선다.
